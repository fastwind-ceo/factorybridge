name: Deploy FactoryBridge to VPS

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Deployment notice
        run: echo "FactoryBridge deployment workflow placeholder. Configure VPS secrets before enabling real deployment."
      - name: Required secrets checklist
        run: echo "Required secrets: VPS_HOST, VPS_USER, VPS_SSH_KEY, DEPLOY_PATH"
